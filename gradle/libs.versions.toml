[versions]
mixin = "0.8.7"
mixinextras = "0.5.4"
geyser = "2.11.2-SNAPSHOT"
auto-service = "1.1.1"
# PackConverter 3.5.8-SNAPSHOT pinned to the personal fork that contains
# the GĐ4.1 + GĐ4.2 fixes (path-traversal in TextureConverter + error
# counting in ConverterPipeline). Without the pin, a fresh pull of
# 3.4.3-SNAPSHOT from repo.opencollab.dev would silently revert both
# fixes — the literal 3.4.3 coordinate is a "floating" snapshot, the
# contents of the .jar under that name change every time GeyserMC CI
# publishes a new build, and the Hydraulic build is non-reproducible.
# To rebuild against a different PackConverter version, either
#   - update the version here and `./gradlew :converter:publishToMavenLocal`
#     on the PC fork then `./gradlew build` on the Hydraulic side, or
#   - pass `-DpackConverterVersion=<coord>` on the command line and run
#     the same two-step flow.
pack-converter = "3.5.8-SNAPSHOT"
indra = "4.0.0"
shadow = "9.4.2"
architectury-plugin = "3.5-SNAPSHOT"
architectury-loom = "1.17-SNAPSHOT"
asm = "5.2"
examination = "1.3.0"

# Fabric
fabric-loader = "0.19.3"
fabric-api = "0.152.2+26.2"

# NeoForge
neoforge-version = "26.2.0.6-beta"

[libraries]
mixin = { group = "org.spongepowered", name = "mixin", version.ref = "mixin" }
mixinextras = { module = "io.github.llamalad7:mixinextras-common", version.ref = "mixinextras" }
geyser-api = { group = "org.geysermc.geyser", name = "api", version.ref = "geyser" }
geyser-core = { group = "org.geysermc.geyser", name = "core", version.ref = "geyser" }
geyser-fabric = { group = "org.geysermc.geyser", name = "fabric", version.ref = "geyser" }
auto-service = { group = "com.google.auto.service", name = "auto-service", version.ref = "auto-service" }
pack-converter = { group = "org.geysermc.pack", name = "converter", version.ref = "pack-converter" }
indra = { group = "net.kyori", name  = "indra-common", version.ref = "indra" }
shadow = { group = "com.gradleup.shadow", name = "com.gradleup.shadow.gradle.plugin", version.ref = "shadow" }
architectury-plugin = { group = "architectury-plugin", name = "architectury-plugin.gradle.plugin", version.ref = "architectury-plugin" }
architectury-loom = { group = "dev.architectury.loom-no-remap", name = "dev.architectury.loom-no-remap.gradle.plugin", version.ref = "architectury-loom" }
asm = { group = "org.ow2.asm", name = "asm-debug-all", version.ref = "asm" }

# Required until pack-converter is updated to use the new examination API
examination-api = { group = "net.kyori", name = "examination-api", version.ref = "examination" }
examination-string = { group = "net.kyori", name = "examination-string", version.ref = "examination" }

# Fabric
fabric-loader = { group = "net.fabricmc", name = "fabric-loader", version.ref = "fabric-loader" }
fabric-api = { group = "net.fabricmc.fabric-api", name = "fabric-api", version.ref = "fabric-api" }

# NeoForge
neoforge = { group = "net.neoforged", name = "neoforge", version.ref = "neoforge-version" }
